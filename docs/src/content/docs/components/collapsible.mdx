---
title: Collapsible
description: A controlled disclosure component built on Radix Collapsible. The trigger and content are separate sub-components that can be positioned independently, and state can be driven programmatically via open / onOpenChange.
---

import Example from "../../../components/Example.astro";
import { CollapsibleBasicDemo, CollapsibleOpenDemo } from "../../../components/CollapsibleDemo.tsx";

<div class="callout">
  <p>
    <strong>Collapsible vs Details.</strong>
    Use <code>Details</code> (native HTML) for simple disclosure patterns — FAQs, inline help, "show
    more" — where you don't need animation or programmatic control. It's zero-JS and
    browser-managed. Use <code>Collapsible</code> when you need: controlled state (<code>open</code>{" "}
    + <code>onOpenChange</code>), animated open/close via <code>data-state</code>, or trigger and
    content that aren't co-located in the markup (e.g. a sidebar toggle button controlling a panel
    elsewhere). Accordion already uses Collapsible internally.
  </p>
</div>

## Basic (uncontrolled)

<Example
  code={`import { Collapsible } from '@eekodigital/raster';\n\n<Collapsible.Root>\n  <Collapsible.Trigger>Audit scope</Collapsible.Trigger>\n  <Collapsible.Content>\n    This audit covers financial controls…\n  </Collapsible.Content>\n</Collapsible.Root>`}
>
  <CollapsibleBasicDemo client:load />
</Example>

## Open by default

<Example
  code={`<Collapsible.Root defaultOpen>\n  <Collapsible.Trigger>Findings summary</Collapsible.Trigger>\n  <Collapsible.Content>…</Collapsible.Content>\n</Collapsible.Root>`}
>
  <CollapsibleOpenDemo client:load />
</Example>

## Controlled

Pass `open` and `onOpenChange` to drive state from your component.

```tsx
const [open, setOpen] = useState(false);

<Collapsible.Root open={open} onOpenChange={setOpen}>
  <Collapsible.Trigger>Toggle</Collapsible.Trigger>
  <Collapsible.Content>…</Collapsible.Content>
</Collapsible.Root>;
```

## Props

### Collapsible.Root

<table class="props-table">
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>open</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>—</td>
      <td>Controlled open state.</td>
    </tr>
    <tr>
      <td>
        <code>defaultOpen</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>
        <code>false</code>
      </td>
      <td>Uncontrolled initial state.</td>
    </tr>
    <tr>
      <td>
        <code>onOpenChange</code>
      </td>
      <td>
        <code>(open: boolean) =&gt; void</code>
      </td>
      <td>—</td>
      <td>Called when state changes.</td>
    </tr>
    <tr>
      <td>
        <code>disabled</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>
        <code>false</code>
      </td>
      <td>Prevents toggling.</td>
    </tr>
    <tr>
      <td>
        <code>className</code>
      </td>
      <td>
        <code>string</code>
      </td>
      <td>—</td>
      <td>Additional CSS classes.</td>
    </tr>
  </tbody>
</table>
