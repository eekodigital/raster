---
title: PasswordToggleField
description: A password input with a visibility toggle button. The toggle switches between type="password" and type="text", with appropriate icons for each state.
---

import { PasswordToggleField as PasswordToggleFieldComponent } from "@eekodigital/raster";
import Example from "../../../components/Example.astro";

## Default

<Example
  code={`import { PasswordToggleField } from '@eekodigital/raster';\n\n<PasswordToggleField label="Password" name="password" />`}
>
  <PasswordToggleFieldComponent client:load label="Password" name="password" />
</Example>

## With hint

<Example
  code={`<PasswordToggleField\n  label="New password"\n  name="new_password"\n  hint="At least 8 characters."\n/>`}
>
  <PasswordToggleFieldComponent
    client:load
    label="New password"
    name="new_password"
    hint="At least 8 characters."
  />
</Example>

## With error

<Example
  code={`<PasswordToggleField\n  label="Password"\n  name="password"\n  error="Password must be at least 8 characters."\n/>`}
>
  <PasswordToggleFieldComponent
    client:load
    label="Password"
    name="password"
    error="Password must be at least 8 characters."
  />
</Example>
